package tn.enicar.controllers;
/*
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;



@CrossOrigin("*")
@RestController
@RequestMapping("/api/groupes")
*/
public class GroupeController {
	/*
	@Autowired
	private GroupeService groupeService;
	
  //----------------------------------------------
	@PostMapping
	public Groupe addGroupe(@RequestBody Groupe groupe) {
		return this.groupeService.addGroupe(groupe);
	}
	@GetMapping
	public List<User> getAllgGroupes(){
		return this.groupeService.getAllUser();
	}
	
	@PutMapping("/{id}")
	public Groupe update(@PathVariable Long id,@RequestBody Groupe groupe) {
		return this.groupeService.updateGroupe(groupe);	
	}
	
	@DeleteMapping("/{id}")
	public void delete(@PathVariable Long id) {
		this.groupeService.deleteGroupe(id);
	}
	@GetMapping("/{id}")
	public Groupe getById(@PathVariable Long id) {
		return this.groupeService.getById(id);
	}*/

}
